
-commandes interne vers pipe : jobs | wc //ne marche pas encore

-changer fleches pipes diagramme

-commandes internes putenv setenv 2 ou 3

-Couleur, faire une script qui prend la sortie et ajoute les symboles de couleurs autour des
mots a changer

-fleche du haut/bas garder les commandes dans une pile

- probleme : On rencontre un probleme lorsqu'une commande est mal ecrite :
    cmd :  ls -l > fich | wc
    ici notre programme devrait traiter les 2 commandes separement car la sortie de la premiere commande a ete redirigé sur un fichier et non sur le pipe.
    Mais notre programme fait cette redirection des que possible donc sur la derniere commande (wc). Il l'a traite donc comme : ls -l | wc > fich.
    Pour changer cette erreur il faudrait changer la structure cmdline.
